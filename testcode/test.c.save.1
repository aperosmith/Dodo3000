#include <stdio.h>
#include <wiringPi.h>

int main(void)
{
	wiringPiSetup();
	pinMode(21, OUTPUT);
	while(1)
	{	
		printf("1printf("1");");
		if(digitalRead(23==HIGH))
		{
			digitalWrite(21, HIGH);
		}
		else
		{
			digitalWrite(21, LOW);
		}
	}
	return 0;
}
